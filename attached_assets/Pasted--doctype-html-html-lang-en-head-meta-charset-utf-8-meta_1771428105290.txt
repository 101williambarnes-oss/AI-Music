<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Hit Wave Media</title>
  <style>
    :root{
      --bg1:#050615;
      --bg2:#06081d;
      --cyan:#6cf0ff;
      --cyan2:#4ddcff;
      --purple:#a06bff;
      --magenta:#ff4fd8;
      --text:#eaf0ff;
      --muted:rgba(234,240,255,.78);
    }

    *{box-sizing:border-box}
    body{
      margin:0;
      font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      background: #000;
      color: var(--text);
    }

    /* HERO / HEADER BANNER */
    .heroBanner{
      position: relative;
      min-height: 360px;
      display:flex;
      align-items:center;
      justify-content:center;
      overflow:hidden;
      padding: 72px 24px;
      background:
        radial-gradient(1200px 600px at 50% 45%, rgba(160,107,255,.18), transparent 62%),
        radial-gradient(900px 500px at 50% 55%, rgba(108,240,255,.14), transparent 60%),
        linear-gradient(180deg, var(--bg1), var(--bg2));
    }

    /* STARFIELD (no images needed) */
    .heroBanner::before{
      content:"";
      position:absolute; inset:0;
      background:
        radial-gradient(1px 1px at 8% 22%, rgba(255,255,255,.65) 50%, transparent 52%),
        radial-gradient(1px 1px at 14% 68%, rgba(255,255,255,.55) 50%, transparent 52%),
        radial-gradient(1px 1px at 20% 35%, rgba(255,255,255,.55) 50%, transparent 52%),
        radial-gradient(1px 1px at 28% 78%, rgba(255,255,255,.45) 50%, transparent 52%),
        radial-gradient(1px 1px at 36% 18%, rgba(255,255,255,.55) 50%, transparent 52%),
        radial-gradient(1px 1px at 44% 62%, rgba(255,255,255,.45) 50%, transparent 52%),
        radial-gradient(1px 1px at 52% 28%, rgba(255,255,255,.60) 50%, transparent 52%),
        radial-gradient(1px 1px at 60% 72%, rgba(255,255,255,.50) 50%, transparent 52%),
        radial-gradient(1px 1px at 68% 40%, rgba(255,255,255,.55) 50%, transparent 52%),
        radial-gradient(1px 1px at 76% 18%, rgba(255,255,255,.45) 50%, transparent 52%),
        radial-gradient(1px 1px at 84% 58%, rgba(255,255,255,.55) 50%, transparent 52%),
        radial-gradient(1px 1px at 92% 34%, rgba(255,255,255,.50) 50%, transparent 52%);
      opacity:.55;
      filter: blur(.2px);
      pointer-events:none;
    }

    /* LEFT + RIGHT GLOW WAVES */
    .wave{
      position:absolute;
      top:0; bottom:0;
      width:50%;
      pointer-events:none;
      opacity:.95;
      mix-blend-mode: screen;
      filter: blur(.0px);
    }

    .wave.left{
      left:-10%;
      background:
        radial-gradient(700px 420px at 30% 55%, rgba(108,240,255,.35), transparent 70%),
        radial-gradient(820px 520px at 35% 65%, rgba(160,107,255,.28), transparent 72%),
        radial-gradient(720px 520px at 25% 40%, rgba(255,79,216,.18), transparent 70%),
        conic-gradient(from 210deg at 35% 55%,
          transparent 0deg,
          rgba(108,240,255,.35) 25deg,
          transparent 60deg,
          rgba(160,107,255,.30) 95deg,
          transparent 140deg),
        conic-gradient(from 240deg at 30% 60%,
          transparent 0deg,
          rgba(255,79,216,.18) 20deg,
          transparent 55deg,
          rgba(108,240,255,.22) 90deg,
          transparent 140deg);
      transform: skewX(-6deg);
    }

    .wave.right{
      right:-10%;
      background:
        radial-gradient(700px 420px at 70% 55%, rgba(108,240,255,.35), transparent 70%),
        radial-gradient(820px 520px at 65% 65%, rgba(160,107,255,.28), transparent 72%),
        radial-gradient(720px 520px at 75% 40%, rgba(255,79,216,.18), transparent 70%),
        conic-gradient(from -30deg at 65% 55%,
          transparent 0deg,
          rgba(108,240,255,.35) 25deg,
          transparent 60deg,
          rgba(160,107,255,.30) 95deg,
          transparent 140deg),
        conic-gradient(from 0deg at 70% 60%,
          transparent 0deg,
          rgba(255,79,216,.18) 20deg,
          transparent 55deg,
          rgba(108,240,255,.22) 90deg,
          transparent 140deg);
      transform: skewX(6deg);
    }

    /* soften center area so text reads clean */
    .centerFade{
      position:absolute; inset:0;
      background: radial-gradient(700px 340px at 50% 55%, rgba(0,0,0,.45), transparent 70%);
      pointer-events:none;
    }

    /* TITLE BLOCK */
    .heroText{
      position:relative;
      text-align:center;
      max-width: 960px;
      padding: 10px 12px;
      z-index: 2;
    }

    .heroText .title{
      margin:0;
      font-weight: 900;
      letter-spacing: 2px;
      text-transform: uppercase;
      font-size: clamp(34px, 5vw, 64px);
      color: var(--cyan);
      text-shadow:
        0 0 14px rgba(108,240,255,.40),
        0 0 38px rgba(108,240,255,.20);
    }

    .heroText .subtitle{
      margin: 12px 0 0;
      font-size: clamp(16px, 2.2vw, 26px);
      color: rgba(120, 220, 255, .90);
      letter-spacing: .4px;
      text-shadow: 0 0 18px rgba(160,107,255,.14);
    }

    /* Optional: make it feel “banner-like” on wide screens */
    .heroBanner{
      border-bottom: 1px solid rgba(108,240,255,.12);
    }

    @media (max-width: 700px){
      .wave{opacity:.75}
      .heroBanner{min-height: 320px}
    }
  </style>
</head>
<body>

  <section class="heroBanner">
    <div class="wave left"></div>
    <div class="wave right"></div>
    <div class="centerFade"></div>

    <div class="heroText">
      <h1 class="title">HIT WAVE MEDIA</h1>
      <div class="subtitle">The Home of AI Music</div>
    </div>
  </section>

</body>
</html>